#!/bin/sh

xColor2=$(xrdb -query | grep '\*color2'| awk '{print $NF}')
xColor7=$(xrdb -query | grep '\*color7'| awk '{print $NF}')

du -a --time ~/Pictures | sort -r -k 2 | awk '{print $4}' | dmenu -l 15 -nb "$xColor2" -nf "$xColor7" -sb "$xColor7" -sf "$xColor2" | xargs -r wal -i 
