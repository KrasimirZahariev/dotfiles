#!/bin/sh

_action=$(printf "Shutdown\nRestart\nLogout" | dmenu -i)
[ -z "$_action" ] && exit

case "$_action" in
  Shutdown) shutdown now ;;
  Restart)  shutdown now -r ;;
  Logout)   pkill -u "$LOGNAME" ;;
esac
