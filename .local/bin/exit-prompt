#!/bin/sh

ACTION=$(printf "Restart\nShutdown\nLogout" | dmenu -i)
[ -z "$ACTION" ] && exit

case "$ACTION" in
	Restart)
		shutdown now -r
		exit;;
	Shutdown)
		shutdown now
		exit;;
	Logout)
		pkill -u "$LOGNAME"
		exit;;
esac
