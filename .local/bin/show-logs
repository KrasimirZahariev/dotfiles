#!/bin/sh

_level=3
if [ -n "$1" ]; then
  _level="$1"
fi

for _boot in $(seq -4 0); do
  printf "############## LOG %s ##############\n" "$_boot"
  journalctl -p "$_level" -x -b "$_boot" | cut -d ' ' -f 1-2,5- | sort | uniq -u
  printf "\n\n"
done
